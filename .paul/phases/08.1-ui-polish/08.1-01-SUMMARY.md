---
phase: 08.1-ui-polish
plan: 01
subsystem: ui
tags: [inter, fontsource, design-tokens, typography, css, vite]

# Dependency graph
requires:
  - phase: 05-design-system-shell
    provides: semantic design tokens (--font-sans applied in base.css), FOUC-safe pre-render in main.tsx
provides:
  - Inter typeface bundled offline (@fontsource/inter), applied via --font-sans
  - Inter-tuned typography tokens (weights 400/500/600/700, --lh-tight, --ls-tight/--ls-normal, --fs-2xl)
  - reviewed spacing scale (--space-0 added)
affects: [08.1-02, future UI/component work]

# Tech tracking
tech-stack:
  added: ["@fontsource/inter@^5.2.8"]
  patterns: ["self-hosted webfont bundled by Vite as same-origin woff2 (CSP/offline-safe); typeface swap is one token + entry import"]

key-files:
  created: []
  modified:
    - package.json
    - package-lock.json
    - src/renderer/src/main.tsx
    - src/renderer/src/styles/tokens.css
    - src/renderer/src/styles/base.css

key-decisions:
  - "Static Inter weights (400/500/600/700) via @fontsource/inter, not the variable font — predictable"
  - "Font imports placed above tokens/base CSS in main.tsx so @font-face registers first; no FOUC regression"
  - "Token + entry only — zero component edits (typography centralized in --font-sans)"

patterns-established:
  - "Bundle-don't-CDN webfonts: @fontsource side-effect CSS imports in the renderer entry → Vite emits 'self' woff2, covered by default-src 'self' (no index.html CSP change)"

# Metrics
duration: ~6min (APPLY 2026-06-24); UNIFY deferred, closed 2026-06-26
started: 2026-06-24T00:00:00Z
completed: 2026-06-24T00:00:00Z
---

# Phase 8.1 Plan 01: Inter Typeface + Typography/Spacing Scale Summary

**Inter is now the app typeface (bundled offline via @fontsource/inter, CSP-safe) with an Inter-tuned token scale — full weight set, tighter heading line-height/tracking tokens, and a reviewed spacing scale — applied app-wide through `--font-sans` with no component edits.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~6 min |
| Started | 2026-06-24 (APPLY) |
| Completed | 2026-06-24 (APPLY); SUMMARY written 2026-06-26 |
| Tasks | 2 auto + 1 human-verify checkpoint |
| Files modified | 5 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Inter bundled and applied (offline, CSP-safe) | Pass | `@fontsource/inter@^5.2.8` dep; 400/500/600/700 imported in main.tsx; build emits latin/latin-ext/cyrillic(-ext) woff2 as same-origin assets; index.html CSP unchanged; system-ui retained as fallback |
| AC-2: Typography scale tuned for Inter | Pass | `--font-sans` leads with 'Inter'; `--fw-regular/medium/semibold/bold`, `--lh-tight: 1.25`, `--ls-tight: -0.011em`, `--ls-normal: 0`, `--fs-2xl: 26px` added; dense 13px base preserved; base.css applies `font-feature-settings: 'liga' 1, 'calt' 1` + `letter-spacing: var(--ls-normal)` |
| AC-3: Spacing scale coherent | Pass | `--space-1..6` (4/8/12/16/24/32) retained; `--space-0: 2px` added; token-level only, no per-component rework |
| AC-4: No regressions | Pass | `npm run typecheck` (node + web) + `npm run build` clean; Inter woff2 in bundle; dark default + FOUC-safe order intact; no component/IPC/schema/behavior change |

## Accomplishments

- App typeface switched to Inter, self-hosted and offline-safe (no CDN, no CSP change), via a single token plus the renderer-entry font import.
- Typography token set rounded out for Inter (full weight ramp, tight line-height/tracking tokens, a 2xl size) and spacing scale reviewed (`--space-0` finer rung added) — all without touching a single component.

## Task Commits

Phase 8.1 was committed as a single `feat(8.1-ui-polish)` commit (PAUL decimal-insertion convention — its own commit, separate from the uncommitted Phase-8 bundle). Per-task atomic commits were not used for this design slice.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `package.json` / `package-lock.json` | Modified | Add `@fontsource/inter@^5.2.8` |
| `src/renderer/src/main.tsx` | Modified | Import Inter 400/500/600/700 above token/base CSS |
| `src/renderer/src/styles/tokens.css` | Modified | `--font-sans` → Inter-first; weight/line-height/letter-spacing tokens; `--fs-2xl`; `--space-0` |
| `src/renderer/src/styles/base.css` | Modified | `font-feature-settings` + `letter-spacing` defaults on body |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Static weights, not variable font | Predictable rendering; smaller per-weight files | Four explicit `@fontsource/inter/N.css` imports |
| Font imports first in main.tsx | Register `@font-face` before token/base CSS | No FOUC; theme pre-render order unchanged |
| Token + entry only | Typography is centralized in `--font-sans` | Zero component churn; foundation for 8.1-02 |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 1 (this SUMMARY) | UNIFY deferred per user request on 2026-06-24; written 2026-06-26 |

**Total impact:** Executed as written. The only deviation was process: UNIFY/SUMMARY was deferred at the user's request so 8.1-01 + 8.1-02 could close together under one phase commit.

### Deferred Items

- This SUMMARY itself was deferred (UNIFY skipped on 2026-06-24); now written alongside 08.1-02-SUMMARY.md to close Phase 8.1.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| User feedback after 8.1-01: "UI not improved — make it more modern" | Expected — 8.1-01 was the font/type foundation only; the visible restyle is 8.1-02. No fix needed; clarified scope split. |

## Next Phase Readiness

**Ready:**
- Inter + the full type/spacing token set are the foundation the 8.1-02 color/elevation/density restyle builds on.

**Concerns:**
- None.

**Blockers:**
- None.

---
*Phase: 08.1-ui-polish, Plan: 01*
*Completed: 2026-06-24 (UNIFY closed 2026-06-26)*
